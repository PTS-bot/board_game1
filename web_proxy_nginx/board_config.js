const BOARD_CONFIG = {
    // ขนาดของช่องตารางแต่ละช่อง (px)
    cellSize: 80,

    // สีของตาราง
    colorLight: '#244424ff', // สีสว่าง (ช่องขาว)
    colorDark: '#696969',  // สีเข้ม (ช่องดำ)

    // สีกรอบกระดาน
    borderColor: '#1C1C1C',
    borderSize: 5
};
