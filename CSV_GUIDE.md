# 📝 คู่มือการตั้งค่าโจทย์ในไฟล์ CSV (เกมหมากรุกคณิตศาสตร์)

ระบบอ่านไฟล์ `.csv` จากโฟลเดอร์เกมของคุณ (เช่น `all_game/math01/img_L4/1.csv`) 
ใน 1 ไฟล์ `.csv` จะต้องมีข้อมูลบรรทัดแรกบรรทัดเดียว คั่นด้วยลูกน้ำ (,) ดังนี้:

`รหัสโจทย์, รูปแบบโจทย์, เวลา(วินาที), คำตอบที่ถูกต้อง, ตัวเลือกที่1, ตัวเลือกที่2, ตัวเลือกที่3, ตัวเลือกที่4`

---

## รูปแบบโจทย์ที่มีในระบบ (Column ที่ 2):
1. **`Objective_Test`** = แบบปรนัย มีตัวเลือกให้กด เลือกตอบจากปุ่ม
2. **`Subjective_Test`** = แบบอัตนัย ให้ผู้เล่นพิมพ์คำตอบลงในช่องแล้วกดส่ง
3. **`true_or_false`** = แบบถูกผิด มีแค่ 2 ปุ่มให้กดเลือก

---

## 🟢 ตัวอย่างการเขียน CSV สำหรับแต่ละรูปแบบ:

### 1. แบบปรนัย 4 ตัวเลือก (Objective_Test)
สมมติว่าในรูปโจทย์คือ "5 x 12 = เท่าไร?" และคำตอบที่ถูกต้องคือ "60" และให้เวลาทำ 30 วินาที

> **ในไฟล์ `.csv` เขียนว่า:**
> `1, Objective_Test, 30, 60, 50, 60, 70, 80`

*คำอธิบาย:* 
- `1` = หมายเลขโจทย์ 
- `Objective_Test` = ปรนัย 
- `30` = เวลา 30 วินาที
- `60` = คำตอบที่ถูกต้อง (จะเอาไว้เช็คกับสิ่งที่เด็กกด)
- `50, 60, 70, 80` = ข้อความบนปุ่มตัวเลือกที่จะแสดงบนจอ (สามารถมีกี่ตัวเลือกก็ได้ สลับตำแหน่งได้ แต่ต้องมีตัวเลือกที่ตรงกับคำตอบที่ถูกต้องด้วย)

### 2. แบบอัตนัย พิมพ์ตอบ (Subjective_Test)
สมมติว่าในรูปโจทย์คือ "จงหาค่าของ x เมื่อ 2x = 24" ให้เวลาทำ 60 วินาที

> **ในไฟล์ `.csv` เขียนว่า:**
> `2, Subjective_Test, 60, 12`

*คำอธิบาย:*
- `2` = หมายเลขโจทย์
- `Subjective_Test` = แบบอัตนัย
- `60` = เวลา 60 วินาที
- `12` = คำตอบที่ถูกต้อง (ไม่จำเป็นต้องใส่ตัวเลือกข้างหลัง เพราะช่องจะเป็นพิมพ์คำตอบ)

### 3. แบบถูกผิด (true_or_false)
สมมติว่าในรูปโจทย์คือ "สมการ 3 + 4 = 7 เป็นจริงหรือเท็จ?" ให้เวลา 15 วินาที

> **ในไฟล์ `.csv` เขียนว่า:**
> `3, true_or_false, 15, True, True, False`

*คำอธิบาย:*
- `3` = หมายเลขโจทย์
- `true_or_false` = แบบถูกผิด
- `15` = เวลา 15 วินาที
- `True` = คำตอบที่ถูกต้อง (อิงตัวอักษรใหญ่เล็กให้ตรงกัน)
- `True, False` = ข้อความบนปุ่มที่จะให้กดเลือก

---

## 📌 สรุปเงื่อนไขสำคัญที่ควรระวัง
1. **รูปภาพ (.png)** และ **โจทย์ (.csv)** ต้อง**ชื่อไฟล์เหมือนกัน** และอยู่ในระดับโจทย์ (`img_L1` ถึง `img_L4`) เดียวกันเสมอ (เช่น `1.csv` คู่กับ `1.png`, `2.csv` คู่กับ `2.png`)
2. ต้องเช็คว่าคำสะกดประเภทโจทย์ตรงเป๊ะๆ นะครับ (`Objective_Test`, `Subjective_Test`, `true_or_false`)
3. ตัวอักษรที่พิมพ์ตอบหรือคอลัมน์เฉลย จะถูกเปรียบเทียบจากตัวอักษรพิมพ์เล็กพิมพ์ใหญ่ด้วย ควรเซฟด้วย UTF-8 เสมอเพื่อไม่ต้องมีปัญหาภาษาไทย
